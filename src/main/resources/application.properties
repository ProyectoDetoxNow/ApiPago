spring.application.name=pago
server.port=${PORT:8080}
server.address=0.0.0.0

# --- PostgreSQL connection ---
spring.datasource.url=jdbc:postgresql://${DB_NEON_HOST}:${DB_NEON_PORT}/${DB_NEON_NAME}?sslmode=require
spring.datasource.username=${DB_NEON_ADMIN}
spring.datasource.password=${DB_NEON_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- Optional: timezone and encoding ---
spring.datasource.hikari.connection-init-sql=SET TIME ZONE 'UTC'
spring.sql.init.platform=postgres

api.carrito.url=https://apicarrito-production-1136.up.railway.app/Api/v1/Carrito
api.usuarios.url=https://apiusuario-production-81bf.up.railway.app/api/usuarios
api.inventario.url=https://apiproducto-production.up.railway.app/Api/v1/inventario